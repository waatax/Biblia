# Biblia 專案深度檢討與執行規劃（v3：多語 / 多版本 + 授權釐清）

> 目標：做一個離線、可長期保存的**多語逐字對照聖經閱讀器**，
> 功能等同信望愛 FHL「逐字對照讀經（cgic201 / read100）」，
> 支援 中文 / 西班牙文 / 英文 並排 + Strong Number。
> 存放於 `C:\Users\User\OneDrive\文件\Antigravity\Biblia`。
> 本文件為給 AI agent（Antigravity）執行前的設計藍圖與檢討清單。

---

## 0. 一句話結論

**公有領域版本 → 整本下載、永久離線保存；受著作權版本 → 不整本抓，改走授權 API 即時顯示。**
閱讀器架構天生支援多版本並排，之後加版本只是多一個資料源。

---

## 1.（重要）版本授權矩陣 — 決定每個版本能怎麼用

| 版本 | 語言 | 授權狀態 | 可否整本下載離線保存 | 取得方式 |
|---|---|---|---|---|
| 和合本 (1919, unv) | 中 | **公有領域** | ✅ 可 | FHL JSON API |
| Strong Number (1890) | — | **公有領域** | ✅ 可 | FHL JSON API |
| **Reina-Valera 1909 (RVR1909)** | 西 | **公有領域** | ✅ 可 | 公有領域資料源 |
| **Reina Valera Contemporánea (RVC)** | 西 | © SBU | ✅ 可 | mrk214/bible-data-es-spa |
| **World English Bible (WEB)** | 英 | **公有領域** | ✅ 可 | 公有領域資料源 |
| King James Version (KJV) | 英 | **公有領域** | ✅ 可 | FHL / 公有領域資料源 |
| Reina-Valera **1960** (RVR1960) | 西 | © 聯合聖經公會 | ❌ 不可整本存 | 僅授權 API 即時顯示 |
| **NIV** | 英 | © Biblica | ❌ 不可整本存 | 僅授權 API 即時顯示 |
| **NVI** (西班牙文 NIV) | 西 | © Biblica | ❌ 不可整本存 | 僅授權 API 即時顯示 |

> 原則：**整本重製受著作權的譯本 = 侵權（即使自用）**，且大量爬取通常違反對方條款。
> 受著作權版本只走「授權、即時、標註出處、不快取整本」的路。

---

## 2. 兩條路線

### 路線一（建議主力）：公有領域替代版本 → 完整合法離線
- 西班牙文：**RVR1909**（RVR1960 的前身，風格高度接近，公有領域）
- 英文：**WEB**（現代好讀，近似 NIV 定位）或 **KJV**（經典）
- 中文：和合本 + Strong

→ 成品：中 + 西 + 英 + Strong 逐字對照，全部合法、離線、永不失效。

### 路線二（要 RVR1960 / NIV / NVI 本身）：授權 API 即時顯示
- 管道：**API.Bible (scripture.api.bible)**、Biblica、YouVersion 等，申請金鑰＋同意條款。
- 限制：多為「即時顯示 + 標註出處」，**禁止整本快取/儲存/再散布**，有流量上限。
- 不符合「整本永久離線」模型；適合「線上查、即時顯示」。

### 務實建議：混合
離線骨幹用路線一（公有領域）；需要對照 RVR1960/NIV/NVI 時，再以授權 API 即時查詢單章顯示，不落地整本。

---

## 3. 資料來源

### 3.1 中文 + Strong + KJV：FHL JSON API
說明頁：`https://bible.fhl.net/json/`
推測端點（**須現場驗證**）：
```
https://bible.fhl.net/json/qb.php?chineses=創&chap=1&version=unv&strong=1&gb=0
```
| 參數 | 意義 | 值 |
|---|---|---|
| `chineses` | 書卷簡稱 | 創、出、太… |
| `chap`/`sec` | 章/節 | 節可省略=整章 |
| `version` | 版本 | `unv`（和合本）；KJV 等看 FHL 支援 |
| `strong` | 內嵌 Strong | `1` |
| `gb` | 繁簡 | `0`繁/`1`簡 |

### 3.2 西班牙文 RVR1909 / 英文 WEB：公有領域資料源
- 常見公有領域聖經資料源提供 JSON/USFM/XML（如 eBible.org、公有領域 Bible JSON 的 GitHub 專案等）。
- **須現場確認**：來源授權標示、書卷/章節對位是否與和合本一致（便於並排）。
- 若 FHL 也提供這些版本，可一併走 FHL API 統一流程。

### 3.3 受著作權版本：授權 API
- `scripture.api.bible` 申請金鑰 → 依條款「即時查詢單章 → 顯示 → 不儲存整本」。
- 前端以「線上模式」呼叫，離線時該版本不可用（符合授權）。

> 各來源可用性與欄位，一律以**實測**為準（本規劃環境無法連線驗證）。

---

## 4. 目錄結構

```
Biblia/
├─ config/
│   └─ books.csv          # 66卷: 編號,全名,API簡稱,約別,章數
├─ raw/
│   ├─ zh_unv/            # 和合本+Strong 原始
│   ├─ es_rvr1909/        # RVR1909 原始
│   └─ en_web/            # WEB 原始
├─ parsed/                # 對位後，一卷一檔，含多版本 + Strong
│   └─ 01_創世記.json
├─ combined.json          # (可選) 全本合併
├─ app/                   # 自製離線多語閱讀器
│   ├─ index.html  ├─ style.css  └─ reader.js
├─ manifest.csv           # 各版本各章 進度/驗證
├─ download.log
└─ Biblia.md
```
**原則：raw 為唯一真相來源，永不覆寫；parsed/combined 皆可重建。**

---

## 5. 多版本資料結構（前端並排的基礎）

以「節」為對位主鍵，各版本掛在同一節下：
```json
{
  "book_no": 1, "book": "創世記", "book_abbr": "創",
  "testament": "OT", "chapter": 1,
  "verses": [
    {
      "sec": 1,
      "versions": {
        "zh_unv": "起初神創造天地。",
        "es_rvr1909": "…",
        "en_web": "…"
      },
      "words_zh": [
        { "word": "起初", "strong": "H7225" },
        { "word": "神",   "strong": "H0430" }
      ]
    }
  ]
}
```
> 僅格式示意。逐字疊層以 Strong 掛在中文（`words_zh`）；西/英為整節對照。
> 受著作權版本**不寫進此檔**，前端以線上模式即時取。

---

## 6. 介面（自製多語閱讀器）

**技術**：單頁 `HTML+CSS+原生JS`，離線讀本地 JSON，雙擊即開。

**導覽區**：書卷/章、上/下一章、**版本勾選（多選並排）**、繁簡、字級。

**閱讀區**：
- 每節多欄並排：中(和合本) | 西(RVR1909) | 英(WEB)。
- 中文欄可切「逐字對照」疊層（中文/Strong/原文/詞形）。
- 點中文詞 → 顯示該 Strong。
- 「純閱讀 ↔ 逐字對照」切換。

**線上模式（選配）**：勾 RVR1960/NIV/NVI 時，前端呼叫授權 API 即時取該章顯示（需金鑰、需網路、標註出處、不快取）。

---

## 7. 實務坑（沿用並適用多版本）

1. read100.html 為動態渲染 → 用 API 不用 .html。
2. API 欄位/Strong 格式須先單章 dump 驗證。
3. Strong 舊約 H / 新約 G，依約別補前綴避免撞號。
4. Strong 內嵌字串 → 拆成 word+strong 陣列。
5. 卷名用 API 簡稱；雙字簡稱易錯。
6. 章數表寫死，全本 **1,189 章** 定錨。
7. 每請求間隔 1.5s、UA、退避重試≤3。
8. 抓一章存一章、已存在跳過（冪等續傳）。
9. **多版本對位**：西/英與中文的節切分可能不同（合節/分節），需做節對位表，對不上的標記待人工核對。
10. 受著作權版本**絕不整本落地**。

---

## 8. 執行流程

**0 前置**：連線確認、讀 robots/條款、建 books.csv。
**1 單章探針**：中文 創1+太1（FHL）、西 RVR1909 創1、英 WEB 創1 各 dump 原始，確認欄位/Strong/節對位。
**2 解析器**：拆 Strong、做多版本節對位。
**3 全本抓取**：各公有領域版本逐章（各 ~1,189），限速/退避/冪等。
**4 合併**：raw→parsed（多版本掛同節）→（可選）combined.json。
**5 前端**：套 app/，驗證並排、疊層、點詞、（選配）線上模式。
**6 驗證**：各版本章數==books.csv；總數==1,189；節對位抽查；Strong 抽查；UTF-8；出 report.txt。

---

## 9. 給 Antigravity AI agent 的指令範本

> 建立多語聖經閱讀器。**公有領域版本才整本下載保存**：中文和合本+Strong（FHL API, version=unv, strong=1）、
> 西班牙文 RVR1909、英文 WEB。先各抓創1（西/英加英太1）dump 原始 JSON 給我確認欄位與節對位，再全本。
> 各請求間隔 1.5s、含 UA 與退避重試≤3、已存在跳過、寫 manifest。
> 解析成「以節對位、多版本掛同節、中文含 word+strong 陣列」的 parsed JSON。
> 前端寫純 HTML+CSS+JS 離線閱讀器：書卷/章導覽、版本多選並排、中文逐字疊層、點詞查 Strong、純閱讀切換。
> **RVR1960/NIV/NVI 屬受著作權版本，不得整本下載或儲存**；若要支援，僅實作「線上模式」透過授權 API
> (scripture.api.bible) 即時取單章顯示、標註出處、不快取，並需使用者自備金鑰。

**鐵律：先各版本單章 dump → 我確認欄位與節對位 → 才寫解析 → 才全本 → 才前端。**

---

## 10. 待你確認

1. **西班牙文**：接受用公有領域 **RVR1909** 做離線主力嗎？（RVR1960 只能線上授權顯示）
2. **英文**：要 **WEB**（現代好讀）還是 **KJV**（經典）當離線主力？可否兩個都要？
3. **受著作權版本(RVR1960/NIV/NVI)**：要不要我實作「線上模式」框架（你自備 API 金鑰）？還是先不做、之後再加？
4. **版面**：多欄並排（中|西|英）可以嗎？預設顯示哪幾欄？
5. **繁簡**：繁體 `gb=0` 即可？

確認後即可產出：① 公有領域版本下載器 ② 多語離線閱讀器前端（含選配線上模式框架）。

---

*本文件為藍圖；API 參數/欄位、公有領域來源可用性、節對位、授權條款一律以官網最新文件與實測為準。*
*規劃環境無法連線外部聖經網站，故來源與介面細節須以實際頁面/實測核對。*
